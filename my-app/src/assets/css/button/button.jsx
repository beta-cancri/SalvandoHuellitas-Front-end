import React from 'react';
import './Button.css'; // Asegúrate de importar tu archivo CSS

const Button = () => {
  return (
    <button className="button">
      Click Me
    </button>
  );
};

export default Button;
